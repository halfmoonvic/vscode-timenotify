import { InsertFormat, NotificationMode } from "./types";

export const DEFAULT_NOTIFICATION_MODE: NotificationMode = "toast";
export const DEFAULT_SNOOZE_MINUTES = 5;
export const DEFAULT_INSERT_FORMATS: InsertFormat[] = ["timestampMs", "timestampSec", "iso"];
